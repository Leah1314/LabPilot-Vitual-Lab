# Codex persistent memory

At the start of a task, read `Codex-Memory/00-System/Context Pack.md`, then follow only the links relevant to the current task. Treat the user's current instruction as authoritative when it conflicts with stored memory.

At the end of a task, do not write routine logs. When a durable decision, explicit preference, project-state change, or reusable lesson appears, update the appropriate file under `Codex-Memory` according to `Codex-Memory/00-System/Memory Policy.md`. Never store secrets or sensitive personal data.

